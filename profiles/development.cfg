[buildout]
extends = base.cfg

[zopeinstance]
debug-mode = on
products =
    ${buildout:directory}/checkout-products
    ${buildout:directory}/parts/development-products
    ${buildout:directory}/parts/silva-all
    ${buildout:directory}/parts/silva-extra-svn
    ${buildout:directory}/parts/silva-extra-dist

