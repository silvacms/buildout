[python scripts]
bin/paster = {bin_prefix}/silva-paster
bin/silva = {bin_prefix}/silva

[shell scripts]

[configurations]
parts/instance/etc/zope.conf = {etc_prefix}/zope.conf
parts/uwsgi/uwsgi.xml = {etc_prefix}/uwsgi.xml
debug.ini = {etc_prefix}/debug.ini
deploy.ini = {etc_prefix}/deploy.ini
mime.types = {etc_prefix}/mime.types

[eggs]
eggs =

[source-eggs]
src =

[ignores]

[replacements]
bin = {bin_prefix}
parts/instance = {var_prefix}/lib/silva
var/instance = {var_prefix}/lib/silva
var/filestorage = {var_prefix}/lib/silva/filestorage
var/uploads = {var_prefix}/lib/silva/uploads
var/cache = {var_prefix}/lib/silva/cache
var/instance.pid = {var_prefix}/run/silva/unused.pid
var/instance.lock = {var_prefix}/lock/silva.lock

[others]
bin/uwsgi = {bin_prefix}/silva-uwsgi
parts/instance/etc/package-includes = {etc_prefix}/package-includes
parts/instance/etc/site.zcml = {etc_prefix}/site.zcml
var/log = {var_prefix}/log/silva
